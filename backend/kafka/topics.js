/**
 * Kafka topic names used across all services.
 */
const TOPICS = {
  HOTSPOT_EVENTS: 'hotspot-events',
  USER_EVENTS: 'user-events',
  NOTIFICATION_EVENTS: 'notification-events',
};

module.exports = { TOPICS };
